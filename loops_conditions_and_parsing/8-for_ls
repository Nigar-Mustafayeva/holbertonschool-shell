#!/usr/bin/env bash
# Display directory contents after removing the part before the first dash

for file in *; do
    # Extract the part after the first '-'
    new_name="${file#*-}"

    echo "$new_name"
done
